---
# Home-lab hosts (Tailscale / LAN). Not provisioned by infra/ Terraform.
# Reachable from your tailnet; gcp_lab is independent (IAP + Cloud NAT).

all:
  children:
    lab:
      children:
        home_lab:

home_lab:
  hosts:
    tottipi:
      ansible_host: tottipi
