---
# Home-lab hosts (Tailscale / LAN). Not provisioned by infra/ Terraform.
# Reachable from your tailnet; CI converge uses --limit gcp_lab only.

all:
  children:
    lab:
      children:
        home_lab:

home_lab:
  hosts:
    tottipi:
      ansible_host: tottipi
