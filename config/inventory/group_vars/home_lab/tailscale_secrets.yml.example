# Rename/copy to tailscale_secrets.yml in this folder (gitignored): variables merge for group `home_lab`
# automatically on every playbook run — no `-e @...` needed.
#
# Tailscale keys: https://login.tailscale.com/admin/settings/keys

tailscale_auth_key: tskey-auth-REPLACE_ME________

# tailscale_advertise_exit_node: false  # default in home_lab/tailscale.yml
# tailscale_accept_dns: true
