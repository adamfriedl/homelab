REPO_URL=https://github.com/adamfriedl/homelab
RUNNER_NAME=tottipi
RUNNER_LABELS=homelab,tottipi
# One-time registration token from GitHub → Settings → Actions → Runners → New self-hosted runner
# Copy to .env, register once, then remove the token (runner state persists in ./data).
RUNNER_TOKEN=
